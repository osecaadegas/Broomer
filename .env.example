# Supabase Dashboard > Connect > Shared Pooler > Transaction mode
# Replace every bracketed value with the values shown by Supabase.
DATABASE_URL="postgresql://postgres.[PROJECT_REF]:[DATABASE_PASSWORD]@[POOLER_HOST]:6543/postgres?sslmode=require"
